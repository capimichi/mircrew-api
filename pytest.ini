[pytest]
addopts = -q
testpaths = tests
markers =
    unit: unit tests (offline)
    integration: integration tests (offline)
    functional: functional tests (real dependencies)
    network: tests that require network access
